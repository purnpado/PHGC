version: "3.8"

services:
  phgc-bridge:
    build: .
    container_name: phgc-bridge
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
